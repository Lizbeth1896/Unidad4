import { ColortitleDirective } from './colortitle.directive';

describe('ColortitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ColortitleDirective();
    expect(directive).toBeTruthy();
  });
});
