import { PaintelementDirective } from './paintelement.directive';

describe('PaintelementDirective', () => {
  it('should create an instance', () => {
    const directive = new PaintelementDirective();
    expect(directive).toBeTruthy();
  });
});
